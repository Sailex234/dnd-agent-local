# URL base de la API publica de hojas (FastAPI). Sin barra final.
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000

# Credencial unica compartida (login web + HTTP Basic hacia la API). Debe coincidir
# con API_AUTH_USER/API_AUTH_PASSWORD de apps/api.
AUTH_USER=changeme
AUTH_PASSWORD=changeme
